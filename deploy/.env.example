# ============================================================
# claw-cage — Environment Configuration
# https://github.com/VelocityPulse/claw-cage
#
# Copy this file to .env and fill in your API keys.
# At minimum, uncomment and set ONE LLM provider key.
# ============================================================

# === LLM Providers (at least one required) ===

# Anthropic (Claude) — primary provider
ANTHROPIC_API_KEY=

# OpenAI (GPT)
# OPENAI_API_KEY=

# OpenRouter — access multiple models via one key
# OPENROUTER_API_KEY=

# Google Gemini
# GEMINI_API_KEY=

# Groq — fast inference
# GROQ_API_KEY=

# Ollama — local models (requires Ollama running on the host)
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# === Gateway ===

# Gateway authentication token (auto-generated if left empty)
# OPENCLAW_GATEWAY_TOKEN=

# Bind gateway to loopback only — access via SSH tunnel
OPENCLAW_GATEWAY_BIND=loopback

# Disable mDNS/Bonjour advertisement on local network
OPENCLAW_DISABLE_BONJOUR=1

# === Channels (optional) ===

# Telegram bot — get a token from @BotFather
# TELEGRAM_BOT_TOKEN=

# WhatsApp — requires WhatsApp Business API setup
# WHATSAPP_ENABLED=false

# === Performance (constrained hardware) ===

# Limit Node.js heap size — uncomment on machines with < 1 GB RAM
# NODE_OPTIONS=--max-old-space-size=384
